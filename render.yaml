services:
  - type: web
    name: voicelog
    runtime: python
    buildCommand: pip install -r "⚙️ 后端/requirements.txt"
    startCommand: cd "⚙️ 后端" && uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONIOENCODING
        value: utf-8
