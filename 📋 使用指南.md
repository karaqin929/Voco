# Voco 使用指南

## 当前用法（线上 Web App）

**线上地址**: `https://kks-voicelog.onrender.com`（支持手机浏览器添加到主屏幕，变成 App 使用）

### 每日流程

1. **ChatGPT Live 口语练习**（或任意英文对话）
2. 生成学习日报：
   - 方式 A：把 `📋 日报模板/ChatGPT日报Prompt.md` 里的模板发给 ChatGPT / Claudian，按新版 JSON 格式生成
   - 方式 B：直接让 Claudian 生成（如「帮我生成一份今天的 GPT 日报，话题是…」）
3. Voco「我的」→「导入日报」→ 粘贴 JSON → 解析入库
4. 打开 App 查看：
   - **首页**：4 维指标（流利度/准确度/自然度/词汇）+「今天需要提升」纠错卡 + 待办
   - **复习页**：今日新词 / 高频错词 / 待复习（SM-2 记忆曲线）
   - **跟读页**：沉浸式播放器，逐句跟读今日核心句型（按住录音、松开结束）

### 底部导航

| Tab | 功能 |
|-----|------|
| 首页 | 仪表盘 + 历史日期切换（点 🐻 查看 8.10/8.12 等历史日报） |
| 复习 | 词库 + 今日新词 + 高频错词 + 待复习（SM-2 闪卡） |
| 跟读 | 沉浸式单卡播放器（核心句型队列 1/N） |
| 我的 | 登录 / 导入日报 / 设置（主题/字号） |

### 历史数据兼容

旧版 Markdown 日报仍然可以导入和查看——App 内置无损清洗层 `normalizeDailyData`，历史日报（8.10、8.12…）渲染时自动补齐结构，数据库原文永不动，一条记录不丢。

---

## 本地开发 / MCP 插件（遗留模式）

> 以下为早期本地服务方案（端口 8765 + Chrome MCP 插件），当前主用线上 App，此模式保留备用。

### 启动服务

双击 `启动.bat`，看到二维码即表示启动成功。

### 安装 Chrome 插件

1. 打开 Chrome → `chrome://extensions`
2. 开启右上角「开发者模式」
3. 点「加载已解压的扩展程序」
4. 选择 `🔌 MCP插件/` 文件夹
5. 插件图标出现在工具栏

### 手机访问（本地模式）

确保手机和电脑同一 WiFi → 浏览器打开 `http://电脑IP:8765` → 添加主屏幕 → 变成 App

## 文件结构

```
📱 产品开发/🗣️ 口语练习/
├── 📋 日报模板/ChatGPT日报Prompt.md   ← 新版 JSON 日报模板
├── 📋 使用指南.md
├── CLAUDE.md                          ← Claude 开发记忆文件（版本/铁律/清单）
├── 🔌 MCP插件/                        ← Chrome 扩展（遗留模式）
├── ⚙️ 后端/server.py                  ← FastAPI 静态文件服务
├── 🖥️ 前端/                           ← PWA（app.js / parser.js / sw.js）
├── 💾 数据/                           ← 本地数据样本与日报归档
└── 启动.bat                           ← 双击启动（遗留模式）
```
