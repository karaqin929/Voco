# Voco — Day 7 开发日志（P1~P4 架构重构 · v55 · voco-v65）

> 承接 08-13 日志（v54 / voco-v64）。本批次为**全局架构重构**（P1→P6 六阶段），
> 按用户批准计划推进：P1 数据层 → P2 路由层 → P3 复习页混合记忆引擎 → P4 跟读页沉浸式播放器收尾 →
> P5 统一端到端集成测试与发布（v55 / voco-v65）。P1~P4 不做中间提交，全套变更一次发布。

## 完成事项

| 阶段 | 内容 | 验收 |
|------|------|------|
| **P1 数据层** | 打标收敛于网关层：`stampDailyTags`/`buildWordSnapshot`/`mergeDemoVocab`（同名行继承布尔标签、缺词永久补入）；`isDueBySrs`/`isMistakeByCrossRef` 纯函数；渲染层只读布尔标签，零 `new Date()` 数据过滤（去时间化铁律） | ✅ 70/70 |
| **P2 路由层** | 规范化路由中枢：`/review?tab=all\|grammar\|due`、`/shadowing?id=xxx`、`/`；`navigateReview`/`navigateShadowing` 导航器 + `navigateToTab` 兼容层 + `normalizeLegacyUrl()` + `handleRoute()` + popstate；server.py SPA catch-all（全部 API 路由之后）；URL 单一数据源 | ✅ |
| **P3 复习页** | **混合记忆引擎**：① 严格三 Tab（全部词汇仅词卡 / 语法错题仅错题卡（原句删除线+正确句高亮）/ 待复习 needsReview 词+错题统一混合卡组）；② Active Recall 双阶段（正面仅英文+音标或仅错句，中央 [👁️ 点击显示答案]，背面 [🔴 没记住][🟢 记住了]）；③ SM-2 驱动（🔴 quality=0 留队列 / 🟢 quality=3 星+1 计算下次复习平滑收起，`_reviewedErrorIds` 会话去重）；④ CSS 布局分离（星点左、按钮右、来源行截断） | ✅ |
| **P4 跟读页** | 沉浸式播放器收尾：① 死代码物理清理（旧 SM-2 入口 banner、startWordsReview、旧三键闪卡 stub 全删，JS/DOM/CSS 三处同步）；② 单卡沉浸式视口（`#speak-player` calc(100dvh-92px) 无列表残影，主句衬线体居中，划线弱化 replacedSentence + 解释胶囊）；③ 底部固定流式交互台状态闭环（[🔊 听原音] 灰次按钮 / [🎙️ 按住录音] 主按钮按压缩放反馈 / [🗣️ 听自己] 未录音 disabled 录音后点亮 / [下一句 →] 递增并重置录音态 / 末句 [🎉 训练完成] disabled）；④ ID 锚定无缝承接（`resolveAnchorIndex`：?id= 驱动首卡，缺失/越界钳制 0） | ✅ 59/59 |
| **P5 测试发布** | 统一端到端集成测试 `.voco-e2e-test.js`（E1 数据层 / E2 路由层 / E3 复习引擎 / E4 播放器四段全量回归，声明抓取器 grabDecl 复用）；版本 v54→v55 / voco-v64→voco-v65；统一提交发布 | ✅ 94/94 |

**Git commit**：`5156d4c`（`ccb6e5b..5156d4c`，main）—— 5 文件 +552/−561。
**线上验证**：`curl -sL https://kks-voicelog.onrender.com/ | grep -o 'app.js?v=[0-9]*'` → `app.js?v=55` ✅

## 🔑 核心机制 1：混合记忆引擎（复习页三 Tab）

- **单一数据源**：`allGrammarErrors()`（真实日报 `_errorsAll` 优先，`isMistake` 词兜底）；`buildDueDeck()`（needsReview 词按 `next_review_date` 升序 + 错题按 `_reviewedErrorIds` 去重）
- **纯函数流**：`applyDueRating`（词→SM-2 字段更新 / 错题→会话去重）→ `flowDueDeck('again'|'good')`（队尾回流 / 平滑收起）→ `rateDueCard`（异步 SB 写入，select 先行守卫，演示态静默兜底）
- **Active Recall**：正面遮罩仅英文+音标（错题卡仅错句），中央唯一 [👁️ 点击显示答案]；背面完整释义 + 双反馈键；未展开不渲染反馈键

## 🔑 核心机制 2：沉浸式播放器状态闭环

| 状态 | 听原音 | 按住录音 | 听自己 | 下一句 |
|------|--------|----------|--------|--------|
| 初始 | 灰次按钮可用 | 主按钮（按压 scale 反馈） | disabled | 可用 |
| 录音中 | 可用 | 缩放高亮 | disabled | 可用 |
| 已录音 | 可用 | 可用 | **点亮** | 可用 |
| 末句 | 可用 | 可用 | — | 🎉 训练完成 disabled |

`handlePlayerNext`：currentIndex+1 → `_playerHasRecorded=false` → releasePlayerAudio + speechSynthesis.cancel（全状态复位）。
句子文本变化时 CSS `playerSwitch` 动画经 reflow trick 重触发（无 React 的 vanilla 实现）。

## 🔑 核心机制 3：声明抓取器（测试基建）

`.voco-*.test.js` 临时测试文件用 `grabDecl` 从 app.js 源码抓取被测函数声明 eval 进测试作用域：
- 感知字符串/注释/模板字面量（显式 tplStack 处理嵌套反引号与 `${}` 插值内花括号）/正则字面量（regexAllowed 启发式 + 字符类跳过）
- DOM stub（getElementById 映射）、window.history/location/speechSynthesis/URL 全局 stub
- 顶层 for 循环 eval（避免声明被锁进回调作用域）；跑完即删临时文件

## 版本与部署

| 项 | 值 |
|----|-----|
| 线上版本 | `app.js?v=55` |
| SW 缓存 | `voco-v65` |
| 部署方式 | push main → Render 自动部署（约 2-3 分钟） |
| 验证命令 | `curl -sL "https://kks-voicelog.onrender.com/" \| grep -o 'app.js?v=[0-9]*'` |

## 文件变更

| 文件 | 变更 |
|------|------|
| `🖥️ 前端/app.js` | P1 打标网关 + P2 路由中枢 + P3 混合记忆引擎 + P4 播放器收尾 + 死代码物理清理（~2850 行） |
| `🖥️ 前端/index.html` | 旧闪卡 DOM 全删（words-review-* 块）+ #tab-speak 精简为播放器容器 + ?v=55 |
| `🖥️ 前端/style.css` | 旧 .words-review-*/.flashcard-*/.speak-actions 等死 CSS 清理 + 词卡布局分离 + #speak-player 单卡视口 + 玻璃拟态交互台 + playerSwitch 动画 |
| `⚙️ 后端/server.py` | SPA catch-all（最后一条路由，支持 /review /shadowing 直达） |
| `🖥️ 前端/sw.js` | voco-v64 → v65 + FILES ?v=55 |
| `🖥️ 前端/parser.js` | **未改动**（铁律） |
| `CLAUDE.md` | 架构笔记刷新（规范化路由/混合记忆引擎/播放器状态闭环/版本 v55） |
| `📋 使用指南.md` | 复习页三 Tab + 跟读页交互台说明 |

## 明日计划

- 移动端验证按压录音与复习卡组交互
- 跟读数据回传（自评 → 句型熟练度）
- 历史 fluency/accuracy 趋势图
- 每日练习提醒（Notification API）
