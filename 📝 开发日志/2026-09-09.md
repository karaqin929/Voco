# 开发日志 2026-09-09（v124）

## 一、v124 批次（用户「发布」授权，已部署 v124/voco-v134，commits 0ed88ca + bbbd6e7）

1. **日报模板新词收录「三条铁轨」（用户反馈「每日新词里很多是自己流利说出的词，如 coverage universe / opportunities」→ 诊断四因：数量上限歧义、helpfulness 偏好、无互动证据门槛、自检漏洞 → 用户定调「宁愿多收录点不要有漏网之鱼 + 把明显问题修掉」）**：rule 7 重写——coreSentences/newWords 均不设数量上限（宁多勿漏），newWords 收录标准 = 「你当天需要帮助才说出的词」：① 必收——有任何求助痕迹（卡壳/问意思/查过/给提示后才说出/说错被纠正，轻微卡壳也算），绝不漏一个真生词；② 排除——独立流利说出、全程无求助的词严禁收进 newWords（再高级再专业也不是生词），独立用出的高级表达以整句形式收进 coreSentences 金句库，不用 newWords 记；③ 拿不准默认收录（宁多勿漏）；严禁编造。自检条目升级为「求助证据」判据。`app.js` TEMPLATES.report 与 `📋 日报模板/ChatGPT日报Prompt.md` 双文件一字不差。用户否决两稿：把 coverage universe/opportunities 当反例写进模板（「太奇怪了」）；先稀疏后放松方向（明确撤回）。
2. **课前教练契约「纠错必做」（用户报「GPT 对话完忘记纠正、忘记给更自然说法、忘记英语老师职责」；用户贴英文版契约对照——结论：英文版是现版浓缩子集、现版无缺口，无需合并任何内容）**：诊断三漏洞——规则 10「自然感永远优先于纠错覆盖率」给漏纠错留了口子（v121 治打断矫枉过正）；「没错不纠」被滥用成「不犯错=整轮不用反馈」；无每轮自检兜底。修复四处：规则 2 加「只要我这轮犯了错，纠错就是必做环节、不可省略，绝不整轮只聊内容不纠错；没犯错就不需要任何反馈，自然继续对话」（用户纠正第一稿「没犯错也极简确认」——「如果没错不用跟我确认」）；规则 3 末尾加「只指出错误、不给正确说法不算完成纠错——每次纠错都必须带上更自然的说法」；规则 10 收口「自然感优先只约束语气和长度——绝不能成为漏纠错的借口」；新增【每轮自检】第 11 条三问自查（先回应内容/有错必纠+给更自然说法/长度克制）。仅 app.js（契约无 md 镜像）。
3. **删除词汇页「复习 +1」快捷旁路（用户问「这个是干嘛的？好像很冗余」→ 确认「删掉，让 SM-2 状态变得干净」）**：f915251（Morandi 重设计）遗留——点一下固定质量 3 跑 SM-2、review_count+1、攒 5 次白嫖「已掌握」。四宗罪：与任务 3 复习页双通道写回、固定质量 3 污染 EF/间隔（没回忆也当「想起」）、5 连点绕过回忆测试、不计复习打卡（两头不沾）。`markMastered` 函数 + 按钮物理删除（grep 仅剩注释引用）；已掌握徽章保留纯展示；btn-small 仍有导入流程「上一步」在用（CSS 保留）。词汇 SM-2 状态变更自此只走复习页四档自评写回，与错题/句型同构。
4. **洞察推荐词汇必须落数组（用户报「洞察推荐 build stamina 但词汇/语法/自然表达/核心句型哪里都没有」；endurance 出现在语法纠正里但漏了词卡）**：诊断两层——App 层：coach_insights 是纯评语文案透传（v105 前端零提取、不入任何表），只有三个结构化数组会入库成卡；Prompt 层：模板从未要求「洞察推荐的词汇必须同时落数组」，GPT 写进洞察就交差。修复：铁律 #8 末尾加链接规则——洞察里推荐的每个具体词汇/整块表达（如 build stamina、incline walking）必须同时在对应数组有落点（求助→newWords / 说错直译→mistakes expression / 高阶→coreSentences），洞察严禁成为唯一归宿；自检两条——洞察词汇落点核查 + 求助词完整性（即使已出现在 mistakes 也要收进 newWords：词卡记生词、错题卡记错误，双落点不冲突）。双文件一字不差。

## 二、发布校验

- **版本三连对齐**：`?v=123→124`（index.html 4 处 + sw.js FILES 5 处）、`voco-v133→134`（sw.js CACHE）、SW 注册 `123→124`（app.js）。
- **node --check**：app.js / sw.js / parser.js / supabase-client.js 四文件全过。
- **全盘交叉审计（用户指令「今天改动太多，怕改了这里错了那里」）**：① 双文件模板逐行比对一字不差（含本批 rule 7 + 洞察链接）；② v123 改动零回退（占位值 0、去锚定铁律、练习交接协议、话题段全部原样）；③ 契约规则 1-11 序号连续、交叉引用「第 3/4 条」全部成立、规则 2/3/10/11 逻辑互洽；④ 词汇页删除零残留（`${btn}`/`markMastered(` 零命中，loadWords 仍有 5 调用方）；⑤ 旧串 `v=123 / voco-v133` 零残留；⑥ 自检 11→13 条双文件同步；⑦ 解析/入库逻辑今天零改动。
- **提交推送**：`0ed88ca`（v124 主体：4 files changed）+ `bbbd6e7`（洞察落点链接：2 files changed）→ push main 成功。

## 三、已知取舍（沿用，记录不修）

- 洞察链接规则只要求「至少一个落点」；求助词完整性自检兜住 endurance 式「只在错题、漏了词卡」的情形——词卡与错题卡双落点属设计内行为，去重不冲突（词汇按词查重、句型按文本查重）。
- 已掌握徽章在词汇列表页保留（纯状态展示，无写回能力），删除的只有旁路按钮。
- 线上验证（curl 轮询）在用户最后确认后进行。
