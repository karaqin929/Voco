# 开发日志 2026-08-26（v116）

## 一、两账号事件（本日主线）

### 时间线
1. **8.25 发现两账号之谜**：patterns/vocabulary/errors/reports 四表分裂在两个 user_id（`1b9ea5bc` 旧账号 8.13~8.22 + `641d79b5` 8.23~8.25），去重 JOIN 全部 0 行的真因。
2. **8.26 上午：错误合并**。当时按「同一用户两个身份」判断，把 1b9ea5bc 全部并入 641d79b5（`merge_accounts_2026-08-26.sql` 记录档）。合并步 ④「重叠日期以当前账号为准」删除了 4 篇重叠日报的旧账号副本。
3. **8.26 下午：用户澄清**——641d79b5 = radiohann@gmail.com 是**另一个人**。立即逆向拆分（`undo_split_2026-08-26.sql`）：用户数据迁回新账号 `0f1b0fbc`（karaqin929@gmail.com），对方数据留在 641d79b5。
4. **8.10 全清**：用户指令「8.10 的可以删掉，把两个账号的都删掉」——两账号 8.10 五批数据（5 句型/15 词/5 错题/1 日报）全删。
5. **守恒终审**：10 行全过——patterns 90+20=110 / vocabulary 50+10=60 / errors 40+14=54 / reports 6+4=10 / progress 用户 20s·50w·18e & 对方 4s·10w·0e。

### 后果与恢复
- 用户 8.21/8.22/8.25 三篇日报**文本**被合并误删（对方账号有副本，但那是对方的数据）。8.17 原文仍在库（id 13）。
- 恢复通道 = **v116 导入日期选择器**：用户从 ChatGPT 复制原文 → 导入时选对应日期 → 入库即查重。待补：8.17 重导 + 8.21/8.22/8.25 三篇。

### 教训（铁律级）
> 库里出现多个 user_id 时，第一步必须是问用户「这个账号是你本人吗？是否有其他人用过？」——禁止擅自合并。拆分全程零 DELETE（只改归属 + 补副本）所以永远可回滚，这是本次能救回来的根本原因。

## 二、v116 四合一（commit e98805e，已部署 v116/voco-v126）

1. **历史日报补导日期选择器**：`#dialog-report-date`（默认今天）+ `_importDateTouched` 手动优先 + `detectReportDate` 文内日期自动识别 + 预览卡明示入库日期；JSON/Markdown 两链路同款改造；**updateProgress/topics 仅在当日导入时执行**（历史导入不重复计 progress）。
2. **updateProgress 口径硬化**：`words_learned` = 词汇表实际行数；`errors_fixed` = max(现值, correct_in_review 行数) 单调护栏。
3. **熊条长按菜单封杀**（用户指令）：`.bear-img` 三行 CSS + `draggable="false"` + contextmenu 守卫（仅 .bear-img）；不用 pointer-events:none，点击查历史日报链路完整保留。
4. **两账号事件 SQL 记录档**六件套入库（merge / undo_split / repair_patterns_fragments / audit_patterns_integrity / audit_errors_integrity / cleanup_errors_broken）。

## 三、线上验证
- `app.js?v=116` / `style.css?v=116` / `sw.js` voco-v126 / `index.html` 引用 v=116 —— 全部 curl 实测通过。
- node --check 三文件（app.js/sw.js/parser.js）全过。

## 四、全盘审计 + v117 修复批次（commit eda0a0d，已部署 v117/voco-v127）

三路并行逐行审计 → 10 处缺陷全部修复（详见 `⚙️ 后端/audit_2026-08-26.md`）：
- 🔴 updateProgress 计数无 user_id 过滤（跨账号污染）→ 已过滤
- 🔴 同日重复导入无查重（覆盖 + 双计）→ 查重拒绝
- 🟠 历史视图复习重置真实 SM-2 曲线 → sm2 移到锚点重绑之后
- 🟠 历史视图复习误写今日打卡戳 → _ctxDate 守卫
- 🟡 日期残留/JSON 裸日期误命中/未来日期/双提交/错题重复行永久到期/月历 || {} 恒真 —— 全部修复
- 线上验证：app.js?v=117 / sw.js voco-v127 / index.html 同步 ✓

## 五、待办
- runGlobalAudit() 浏览器实测（用户跑，我判读）。
- 数据层两账号终审 SQL（模块四，只读预览先行）。
- 历史日报补导（Task #50）：8.21/8.22/8.25 从 ChatGPT 原文导入（8.17 文本仍在库 id 13，无需重导）+ progress 计数同步 SQL。
