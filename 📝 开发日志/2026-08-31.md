# 开发日志 2026-08-31（v121）

## 一、v121 批次（用户「可以了，提交吧」授权，已部署 v121/voco-v131）

本批次为用户逐条提出、逐条修改的「4 问题修复批次」：前半程按用户指令「改完不要升级版本号」只改不推，全部改完确认后统一提交发布。

1. **每日句型队列快照冻结（用户报「打卡数量做一半点去其他页面不断增多、永远做不完」）**：队列此前每次进页面都现场重捞——已复习到期卡的 SM-2 写回异步未落地会原地复活、新卡预算每轮重抽 10 张（复习掉的预算槽被新句回填）、中途导入的日报句随时挤入 → 数字只增不减。修复：新增 `getSpeakQueueForToday` 作为今日队列唯一入口——当天第一次队列成型（live>0）时把 id 列表冻结进 localStorage `voco-speak-queue-<今日>`（条目 {t:'pat',id} / {t:'anchor',text}，重建时按 id 从 _patternLibrary 反查、SM-2 字段恒新鲜、库行已删则静默跳过）；之后当天一切重建只认快照；`rateSentenceCard` 答一张删一张（出队即重写；历史视图复习绝不碰今日快照）；快照=[] 即完成态、绝不回退实时重捞（live=0 时故意不冻结——分不清库未加载还是真没卡）。**队列内容 100% 仍由 `getDueSentencesQueue`（needsReview 到期标记 + next_review_date 升序 + 新卡预算 10 张）决定——记忆曲线零改动，冻结的是「当天任务清单」而非砍数量；新导入句次日进队**（用户专门确认过这一点）。/shadowing 今日路径、锚定句、任务 2 数字、审计模块二/五全部换走新入口（模块八保留现场捞以测真实到期态）。
2. **提示词模板升级（用户：纯场景提示词起不到提示作用）**：v96 起 coreSentences 的 explanation 规范被换成「使用场景或提示」（场景-only），expression 反方向换成「什么场景下用」→ 库里 8.13~8.18 核心卡带词汇点/句型、8.19 起纯场景。改回并升级：核心句型 explanation 必须写「为什么这个更地道」——①重点词汇与固定搭配 ②句型结构/句式骨架；地道表达 explanation 必须写「为什么这样说更好」——重点词汇与固定搭配、句型结构；两类都加「只写场景、不给词汇与句型提示的 explanation 视为不合格」——`app.js` TEMPLATES.report 与 `📋 日报模板/ChatGPT日报Prompt.md` 双文件一字不差（各 4 处）。
3. **课前教练契约重构（用户报「用那个 prompt 练习总是被打断」，给 key points 要求「融入优化而非直接替换」）**：旧规则 1「每说完一句先点出问题再接着聊」= 打断病根。重排为「对话第一，纠错第二」9 条：①绝不打断/不抢话/不接话/不代说完，等完整意思清楚表达完再开口；②先像聊天对象回应内容（回答问题/讨论观点/自然反应），回应完再给简短反馈；③反馈只纠真实错误（有错就纠、没错不纠、绝不编造挑刺）+ 一两句原因 + 更自然说法；④引导与深挖（提示词引导、升级挑战）；⑤点破错误根因——教的是英语背后的思维，不是一条条修补；⑥适时小结 error pattern；⑦复述练习协议（安静听完整复述 → 先评内容清晰完整 → 与上次对比 → 再纠英文 → 最后 1-2 个下次优先点）；⑧课后日报衔接照旧；⑨节奏语气照旧。人设升级为「口语私教 + 长期对话伙伴」、目标「通过真实自然的对话流利地道，而不是上课」；全程英文保留。
4. **答疑两条（无代码改动）**：「云端剩余 10 张」= 任务 3 副标题——本地完成戳已打但云端 due 队列尚未归零（写回延迟/中途导入新 due 卡）时的同步提示，v115 起完成戳与写回脱钩的既定设计；「日报出现对方内容」= 8.21/8.22 两账号共用旧账号（1b9ea5bc）时期数据归属问题——8.26 拆分时按日全量归属用户（p 例句定位 id 157/175/135），非新污染（8.27 补导零新增、对方账号零交叉），用户决定不清理。

## 二、发布校验

- **版本三连对齐**：`?v=120→121`（index.html 4 处 + sw.js FILES 5 处）、`voco-v130→131`（sw.js CACHE）、SW 注册 `v119→121`（app.js L5522——v120 批次漏 bump 的注册串漂移，本次顺手对齐）。
- **node --check**：app.js / sw.js 全过；parser.js / supabase-client.js 本批未动。
- **残留扫描零残留**：grep `?v=120 / voco-v130 / sw.js?v=119` 前端目录零命中（app.js 内 v120 字样均为历史注释，不参与缓存）。
- **工作区干净**：本批改动 = app.js + index.html + sw.js + 日报模板 md + CLAUDE.md + 本日志，无夹带。

## 三、已知取舍（沿用，记录不修）

- 完成戳与写回成功脱钩（v115 设计）——「云端剩余 N 张」即此设计下的可见提示，非缺陷。
- 新卡池逐日消化 10 张/日；v121 起当天队列冻结后中途导入的新句次日进队。
- 8.21/8.22 两日数据归属：按日全量归用户，对方账号无交叉（用户决定不清理、不重导）。
