# 开发日志 2026-09-08（v123）

## 一、v123 批次（用户「改完+审计完之后再发布」授权，已部署 v123/voco-v133，commit db319f0）

1. **错题卡正面提示词直搬（用户报「到期错题里仍有很多没有显示提示词的，翻页后发现有提示词段落，是不是代码又有问题」）**：三层诊断证明非数据问题——errors 表 schema 只有 `rule` 一列（无 explanation）；错题卡正面自 v99 起就从不渲染 rule（中性引导设计，从未升级过）；背面一直渲染 rule。SQL 实测用户账号 76 错题 / 28 到期 / **到期且 rule 为空 0 张**，数据干净无需修复。本地修复：`showDueCard` 错题正面改为与句型卡 v115 同构——原句小字灰显对照 + rule 灰底框直搬，rule 为空才回落中性「回忆正确的英文表达」；正反提示词重复可接受（v115 先例，用户明确）。
2. **日报模板评分示例去锚定（用户问「现在的分数是每天动态打分不是写死的吧」→ SQL 实测确认动态）**：10 天四维分数一行不重复（语法 5.5~6.4 波动最大、词汇 6.5~7.2 最稳），实锤 GPT 逐日真实打分。但示例值 7/6.5/6/7 存在锚定效应（分数挤在 6-7 带）→ 示例结构中的 speakingRatio/fluency/accuracy/naturalness/vocabulary 全部改**占位值 0**、weak_areas 改「弱点标签1, 弱点标签2」，并在【评分与点评铁律】新增占位符声明条 +【输出前自检】新增对应检查项；【字段结构铁律】#2 的 worked example 62→60。`app.js` TEMPLATES.report 与 `📋 日报模板/ChatGPT日报Prompt.md` 双文件一字不差（.md 头部版本标注更新 v123）。
3. **课前教练契约「练习交接协议」（用户报「GPT 让我练习，结果立刻接下一个话题，prompt 好像存在很严重的问题」）**：诊断 = 真实缺口——三处练习布置（规则 3 邀请重说 / 规则 4 升级挑战 / 规则 7 复述）只写了布置动作，全篇没有一条规定「布置完必须停下等用户完成」；规则 9「自然感优先」+ 话题段「卡壳用新问题拉回」在练习等待期（沉默）把 GPT 推向接新话题。修复三处：① 契约新增【练习交接协议】第 8 条——布置练习后必须停下等完成，完成前严禁续聊/切话题/新问题填场（原 8/9 顺延为 9/10）；② 话题段「跑题/卡壳」拆分处置——跑题顺内容带回主题、卡壳不换话题只给提示词引导说出正确句（原「卡壳用新问题拉回主题」与契约第 1/4 条冲突，系帮凶）；③ 话题段补「静默准备练习不算卡壳——练习交接期间等我完成再继续」。
4. **反照抄措辞修正（顺手修复）**：日报模板【评分与点评铁律】反照抄条款写「禁止照抄示例值 … / "时态, 单复数"」但示例里 weak_areas 实际是「时态, 冠词」——警告值与示例值对不上。双文件同步改为与示例一致的措辞（本批 ② 中随占位化一并收敛为「禁止照抄占位值 0 / "弱点标签1, 弱点标签2"」）。

## 二、发布校验

- **版本三连对齐**：`?v=122→123`（index.html 4 处 + sw.js FILES 5 处）、`voco-v132→133`（sw.js CACHE）、SW 注册 `122→123`（app.js L5602）。
- **node --check**：app.js / sw.js 全过。
- **残留扫描零残留**：grep `v=122 / voco-v132` 前端目录零命中；`v=123` 计数 index 4 / sw FILES 5（单行）/ app 1。
- **「只改了一半」专项审计**：grep 拉回主题 / 练习交接 / 卡壳 全仓核查——旧「我跑题或卡壳时，用新的问题把我拉回主题」零残留（仅存注释引用）；练习交接协议出现在契约第 8 条 + 话题段 + 三处注释；规则序号 8/9/10 顺延无断档；日报模板 md 与 app.js 一字不差（课前契约仅存 app.js，无 md 镜像）。
- **提交推送**：commit `db319f0`（`4bbf5cc..db319f0`，4 files changed，49 insertions / 31 deletions）→ push main 成功。
- **线上轮询验证全绿**：index.html 服务端 `v=123` ×4；sw.js `voco-v133`；app.js 新契约标记「练习交接协议」×2 + 新话题段「我跑题时，顺着刚才的内容自然把话题带回主题」×1 全部命中。

## 三、已知取舍（沿用，记录不修）

- 锚定加固后分数带宽预计放宽（GPT 无 6-7 锚点），复盘规则引擎用相对变化（连续下降/四维最低），不受绝对值影响。
- 另一账号（641d79b5）43 张错题全部到期未复习，属该账号自己的队列，与用户数据无关。
- 日报库 `reports.content` 存 GPT 原文（含弯引号键名的历史行）——App 读取时统一清洗，SQL 直查需正则绕过（本批诊断时已验证），入库策略照旧。
