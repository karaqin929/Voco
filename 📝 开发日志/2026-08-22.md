# Voco — Day 13 开发日志（v110 熊条首熊左对齐 · voco-v120）

> 承接 08-21（v105–v109 五连批：私教洞察 + 熊条锚定硬化 + 全局体检 + 熊条格子左对齐 + 横幅/洞察双修复）。
> 本日单批：**v110**（熊条首熊左对齐——熊脸对齐 lucide 图标，用户 Bug 报告驱动）。

## 完成事项

| 版本 | 事项 | 验收 |
|------|------|------|
| **v110** | **熊条首熊左对齐**：v108 的 `-ml-1` 只把「格子」左缘对齐图标，但小熊图 w-6（24px）在 14.28% 宽格（~48px）内被 `items-center` 居中 → 熊脸左缘仍比图标缩进约半格宽（~12px）。修复：`days.map((d, i) => …)` 首格 `items-start`、日期标签首格 `text-left`，熊脸左缘 === 「📅 打卡日历」lucide 图标左缘同一条垂直线；其余格保持居中；pl-1/-ml-1 光圈缓冲与滚动锚定/恢复逻辑零改动 | ✅ curl 线上实测（v110/voco-v120 双生效）+ 用户待视觉确认 |

**Git commits**：`40bc9bc` v110。

## 🔑 核心机制：两级对齐的盒子数学

**两级问题，两批修复**（v108 → v110）：

1. **格子级**（v108）：容器 `pl-1`（防 scale-110 光圈裁切）+ `-ml-1`（负外边距补偿）→ 首格左缘 = 图标左缘；
2. **内容级**（v110）：格子对齐后，熊图仍在格内居中 → 熊脸左缘 = 图标左缘 + (格宽−图宽)/2 ≈ +12px。首格改 `items-start`/`text-left` 后，熊脸左缘 = 格子左缘 = 图标左缘，严格同线。

**附加核验**：bear PNG 192×192 全画布不透明（RGBA alpha bbox = 全图）——熊脸即图缘，无图内透明边距，CSS 对齐即视觉对齐，无需再猜像素补偿。

## 兼容与行为说明

- 首格左对齐只在滑到最左边界时可见（默认锚定最右到今天）；其余格与滚动行为（`_streakAnchored`/`_streakScrollCache`）完全不受影响。
- 首熊选中时 scale-110 左伸 1.2px，仍在 `-ml-1` 外扩的 4px 缓冲带内，光圈完整可见。
- **待用户侧**：① F5 硬刷新（SW voco-v120 自动接管后旧缓存即清）后滑到熊条最左，确认首熊熊脸与「📅 打卡日历」图标左缘同线；② 导入今日（8.22）日报后重跑 `runGlobalAudit()` 应 27/0/0。
