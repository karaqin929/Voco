# Voco — Day 13 开发日志（v110 熊条首熊左对齐 + v111 全局解除截断 · voco-v121）

> 承接 08-21（v105–v109 五连批：私教洞察 + 熊条锚定硬化 + 全局体检 + 熊条格子左对齐 + 横幅/洞察双修复）。
> 本日双批：**v110**（熊条首熊左对齐——熊脸对齐 lucide 图标，用户 Bug 报告驱动）→ **v111**（全局解除截断，用户产品逻辑反转指令驱动）。

## 完成事项

| 版本 | 事项 | 验收 |
|------|------|------|
| **v110** | **熊条首熊左对齐**：v108 的 `-ml-1` 只把「格子」左缘对齐图标，但小熊图 w-6（24px）在 14.28% 宽格（~48px）内被 `items-center` 居中 → 熊脸左缘仍比图标缩进约半格宽（~12px）。修复：`days.map((d, i) => …)` 首格 `items-start`、日期标签首格 `text-left`，熊脸左缘 === 「📅 打卡日历」lucide 图标左缘同一条垂直线；其余格保持居中；pl-1/-ml-1 光圈缓冲与滚动锚定/恢复逻辑零改动 | ✅ curl 线上实测 + 用户待视觉确认 |
| **v111** | **全局解除截断**（用户产品逻辑反转：内容完整 > 排版克制）：① 洞察行 `-webkit-line-clamp:3` 彻底废除 → 块级自然折行 + `break-words`，三四行全文展示；② 例证区（原句/修改/地道/金句）`truncate` 撤销 → `break-words leading-relaxed` 自然折行，全站零省略号；③ 话题卡词条行 `overflow-hidden whitespace-nowrap` → `flex-wrap`（芯片不再被裁）；④ `.badge-source` 去 ellipsis/nowrap → 正常折行；⑤ 容器核实：`card()` 白卡与 `bodyBox()` 灰底盒无固定高度（height:auto），无需改动 | ✅ curl 线上实测（v111/voco-v121 双生效 + grep 复核截断清零）+ 用户待视觉确认 |

**Git commits**：`40bc9bc` v110、`7874ce0` v111。

## 🔑 核心机制 1：两级对齐的盒子数学（v110）

**两级问题，两批修复**（v108 → v110）：

1. **格子级**（v108）：容器 `pl-1`（防 scale-110 光圈裁切）+ `-ml-1`（负外边距补偿）→ 首格左缘 = 图标左缘；
2. **内容级**（v110）：格子对齐后，熊图仍在格内居中 → 熊脸左缘 = 图标左缘 + (格宽−图宽)/2 ≈ +12px。首格改 `items-start`/`text-left` 后，熊脸左缘 = 格子左缘 = 图标左缘，严格同线。

**附加核验**：bear PNG 192×192 全画布不透明（RGBA alpha bbox = 全图）——熊脸即图缘，无图内透明边距，CSS 对齐即视觉对齐，无需再猜像素补偿。

## 🔑 核心机制 2：内容完整 > 排版克制（v111，产品铁律新立）

**用户指令原文**：「宁可卡片在垂直方向上变长，也绝对不能吃掉用户哪怕一个标点符号。」

**四刀清零 + 一处核实**：
1. 洞察行：v109 的 line-clamp:3 是多行截断的**最后一道闸**——废除后 `<span class="flex-1 min-w-0 break-words">` 纯块级折行；
2. 例证区：`origLine`/`targetLine`（原句/修改/地道/金句四标签共用）`truncate` → `break-words leading-relaxed`，词汇行「3 词 · 连接」同步去 truncate；
3. 话题卡词条行：芯片行原来是 `overflow-hidden whitespace-nowrap`（超宽芯片被静默裁掉，比省略号更隐蔽）→ `flex-wrap` 换行展示；
4. `.badge-source`（语法卡来源徽章）：原生 `text-overflow:ellipsis` → `white-space:normal + overflow-wrap:break-word`；
5. 容器：`card()` 白卡 / `bodyBox()` 灰底盒本就 height:auto 零固定高度——实测核实无需改。

**收口复核**：全前端 grep `truncate|line-clamp|ellipsis|text-overflow` 仅剩注释提及，实际样式零残留。此规则已写入 CLAUDE.md 铁律区，**覆盖 v105 单行截断 / v109 例证区保持截断两条历史指令**。

## 兼容与行为说明

- 首格左对齐只在滑到最左边界时可见（默认锚定最右到今天）；首熊选中时 scale-110 左伸 1.2px，仍在 `-ml-1` 外扩的 4px 缓冲带内，光圈完整可见。
- v111 后卡片高度随内容增长——四卡网格纵向错落是**预期行为**（产品已确认宁可变长）。进度条、熊条日期标签、按钮等非内容句元素的 nowrap 按设计保留，不在废除范围。
- **待用户侧**：① F5 硬刷新（SW voco-v121 自动接管）后验证：洞察长文本多行完整展示、四卡例证句零省略号；② 导入今日（8.22）日报后重跑 `runGlobalAudit()` 应 27/0/0。
