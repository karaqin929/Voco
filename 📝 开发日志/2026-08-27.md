# 开发日志 2026-08-27（v120）

## 一、v120 批次（用户「先做整体审计然后发布」授权，已部署 v120/voco-v130，commit 398ee25）

1. **句型队列重排（Q1 收官，用户决定「到期优先 + 新卡限量」）**：`getDueSentencesQueue` 到期卡（next_review_date 升序）全部出队、不设上限——防遗忘是 SM-2 第一要务；新卡殿后按 date_added 降序、每日最多 10 张（当天导入句天然排新卡预算最前）；总安全阀 40 仅防极端情况。取代 v77 起 15 句漏斗（新卡优先截断 → 老卡排队无期）。打卡完成 = 队列练完 = 到期卡清零 + 当日新卡预算消化，任务 2 数字与队列同函数同源。
2. **错题卡防重复守卫**：`dueErrorCards` 新增「原句|正句」判重——库中当前零重复（SQL 实测），纯防御未来重复导入；写回侧 v117 F9 已把重复行一并推进，只拦展示、零行为变化。三条打卡链路（句型/词汇/错题）至此全部有展示侧判重。
3. **灵感舱话题开场指令（用户检查发现话题标签只是简单拼接）**：注入从「贴标签」升级为「开场指令」——GPT 第一句必须是该主题开放性问题（禁无关寒暄）+ 深挖 + 跑题拉回；对齐话题复盘 Prompt 已有的「你先向我提问吧」。
4. **不自然表达分析重构（用户反馈「高频句式 TOP 5 不知道想表达什么」）**：原 TOP 5 在数据量小时全是 ×1 单次句、信息量为零 → 删除，改为「各根因典型句式」——句式挂根因（pattern）名下作例证，每根因最多 2 条；点击详情标注所属根因。

## 二、发布前整体审计（用户指令「先做整体审计」）

- **git diff 全量审读**：58 增 / 27 删全部为既定 4 项改动 + 审计断言/注释同步，零夹带改动。
- **残留扫描零残留**：`PATTERN_SESSION_CAP` / `_expressionStats.top` / 「高频不自然句式」/ 旧话题注入文案全部物理清除（仅注释提及旧版）；index.html 无「15 句」文案（仅时长选择器 15 分钟，无关）。
- **核心函数逐行核实**：getDueSentencesQueue（needsReview/incomplete 双门 + 文本去重 + 到期先行 + 新卡 10 + 安全阀 40）；dueErrorCards（内容门 + 原句|正句判重）。
- **同源核查**：任务 2 数字（L1158）、审计模块二（L2200）、模块五（L2451）、模块八（L2530）、复习页（L3763）全部消费同一函数，口径不分裂；审计模块八断言已换 PATTERN_QUEUE_HARD_CAP。
- **语法弱点分布复核**（用户顺带问）：设计自洽（三类桶唯一聚合入口 + 发音行过滤 + 碎片合并防虚高），未改动；两个小瑕疵记录在案：详情弹 toast 只显示 5 条、已纠正% 依赖 correct_in_review（打卡不写它，v99 设计）。
- **校验与线上验证**：node --check 三文件（app.js/sw.js/parser.js）全过；轮询第 3 次全绿——index.html 4× v=120、sw.js voco-v130、app.js 新常量标记命中。

## 三、待办

- 历史日报补导（Task #50）：8.21/8.22/8.25 从 ChatGPT 原文导入（8.17 文本仍在库 id 13）+ 补导后 progress 计数同步 SQL。
