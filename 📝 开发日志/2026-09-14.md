# 开发日志 2026-09-14（v126：v125 + v126 合并部署）

## 一、v125 批次（契约精简 + 锚句 + 模板铁律 #9）

1. **课前教练契约精简 11 条 → 9 条（用户报「v124 契约后 GPT 整场忘记纠正、被提醒后也不按先回应再纠错的顺序」→ 诊断：长对话指令衰减 + 位置效应——契约在 prompt 最前、话题段在最后，纠错规则注意力最弱；规则越长衰减越快）**：原 2/3 合并为规则 2（先回应内容 → 有错必纠每轮必做 / 没错不反馈 → 解释原因 + 更自然说法，七要点零遗漏）；原【每轮自检】11 删除（自查式规则长对话最先失效，职责由 prompt 末尾锚句取代）；原 4-9 顺延为 3-8、原 10 → 9（节奏与语气，删与 2 重复的收尾句「自然感与纠错一个都不能少」，保留纠错不得豁免）；话题段交叉引用「与对话契约第 4 条一致」→ 第 3 条（引导者重新编号）。
2. **prompt 末尾【每轮固定格式】锚句**（唯一装配点，开场语之后）：「先自然回应我的内容 → 我这轮有错就纠：一两句原因 + 更自然的说法 → 继续对话；我这轮没犯错就不纠、不反馈」——近因效应把纠错循环放在注意力最强位置。
3. **日报模板铁律 #9 explanation 语言风格（用户「英文只保留在具体词汇、语法错误、固定搭配或句型骨架本身等等」）**：所有 explanation 不要写全英文——中文解释，英文只保留在具体词汇、语法错误、固定搭配或句型骨架本身等等；自检同步一条。用户否决带固定例句的第一稿（「太固化」）；git 考古证明旧版模板从未规定语言——风格是模板中文语境自然形成，无旧文案可恢复。app.js TEMPLATES.report 与 `📋 日报模板/ChatGPT日报Prompt.md` 双文件一字不差。

## 二、v126 批次（复习每日配额 20 + 合并 FIFO + 继续加练）

1. **复习每日配额 20+20 张（用户报「一周没打卡回来单词复习 108 张，太劝退」→ 设计确认：混合卡组 20 + 句型 20 = 40/天、按到期日混合 FIFO、继续加练按钮要）**：`DAILY_REVIEW_QUOTA = 20`。`buildDueDeck` 改为 `buildDueDeckMerged()`——词 + 错题按到期日**合并 FIFO** 取前 20：词按 next_review_date、错题由 dueErrorCards().ref 挂原始行同源取日期、无日期 = '0000-00-00' 排最前（新卡立即到期语义），根治旧「先全部词、后全部错题」排序下词积压 ≥20 时错题永不出队的饿死问题；词侧加 `isDueBySrs` 日期重核（needsReview 是加载时打的标签，会话内已复习词自动出队）。句型侧拆 `getDueSentencesQueueRaw`（到期 FIFO 全量 + 新卡预算 10，原函数体逐字保留）/ `getDueSentencesQueue`（Raw 前 20）。
2. **SM-2 曲线零改动：配额 = 延迟 ≠ 跳过**——没轮到的卡原地排队，间隔从实际复习日起算（Anki 同款积压模型，自愈式：久拖还记得 → 间隔跳更大；忘了 → quality 0 重进学习阶段，是真实的遗忘不是数据损坏）。写回层 reviewWordItem/reviewErrorItem/reviewPatternItem 零改动。
3. **打卡 = 配额完成 + 「继续加练」按钮（两条复习流同构）**：任务 3 完成判定 `mergedDueCount === 0 ? deckReviewed > 0 : !!vocabDoneStamp`——积压不再阻止打卡（不劝退核心诉求）；面板改配额口径「今日 20 张 · 队列还有 N 张（约 X 天消化完）」，任务 2 副标题同款「队列还有 N 句」，due Tab / 审计 2.3 / 断言 C 全部同源 min(20, 合并全量)。endDueReview 积压 >0 显示「今日配额复习完成！」+「继续加练（再来 N 张）」（renderDueDeck 重捞，日期重核天然无重复）；句型侧 showSrsDone 今日视图同款——新增 `voco-speak-queue-done-<今日>` 已练记账（pat|id / anchor|text，答一张记一张）+ `continueSpeakQueueExtra()` 加练组重捞（Raw − 已练 − 快照在队，追加进今日快照续练）；历史视图只读无加练。加练多做的题同样正常写回 SM-2。
4. **全盘审计 18 项通过 + F1-F3 处理（用户「做完后全盘审计」→「处理」）**：F1 断言 C 队列纯净检查改 Raw 全量口径（对外切片会收窄巡检半径，主守卫断言 B 全库隔离不变）；F2 `getTodayMissionState` 死字段物理删除（realVocab/dueVocabList/totalDueVocabCount/isReviewFinished 消费者归零，收敛为 hasRealTodayReport + reviewedVocabToday）；F3 v123 历史注释补「v125 契约重编号后现为第 3 条」。F4（CLAUDE.md 两处）随本批文档完成；F5 设计内不改。

## 三、发布校验

- **版本三连对齐**：`?v=124→126`（index.html 4 处 + sw.js FILES 5 处）、`voco-v134→136`（sw.js CACHE）、SW 注册 `124→126`（app.js）。
- **node --check**：app.js / sw.js / parser.js / supabase-client.js 四文件全过。
- **旧串零残留**：v=124 / voco-v134 全仓库 grep 零命中。
- **双文件模板**：铁律 #9 + 自检条 app.js 与 `📋 日报模板/ChatGPT日报Prompt.md` grep 逐字一致。
- **全盘审计**：git diff 逐 hunk 审（v125+v126 全部改动符合设计意图、无意外混入）+ 18 项检查通过（契约 1-9 序号连续、合并七要点零遗漏、删除零残留、锚句唯一装配点、交叉引用第 3 条语义匹配、配额口径全站点同源、合并 FIFO dueKey 有效、_reviewedErrorIds 格式一致、Raw 函数体逐字保留、快照/done 记账 key 规则一致、SM-2 写回零改动、rateDueCard→endDueReview 链完好、版本串三处一致、v124 批次零回退）。

## 四、已知取舍（沿用，记录不修）

- 任务 2「队列还有 N 句」= Raw − 快照长度：当天中途导入日报会使 Raw 变大 → 该数字可能微涨（任务数字本身因快照冻结只减不增）——如实反映新句在排队，设计内行为。
- 配额口径下审计断言 C 已改 Raw 全量（F1），主守卫断言 B 查全库 broken 隔离不变。
- 完成戳与写回脱钩设计不变（v115）：打卡印章 = 配额完成，云端写回延迟不影响打卡。
