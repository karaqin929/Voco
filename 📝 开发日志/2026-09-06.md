# 开发日志 2026-09-06（v122）

## 一、v122 批次（用户「发布」授权，已部署 v122/voco-v132，commit 4bbf5cc）

1. **句型队列删除 40 张总安全阀（用户报「句型复习 40 句 vs 今日到期复习 67 张」SM-2 审计后决定「删除安全阀」）**：SQL 实测真实到期 99 句（复习过到期 36 + 从未复习 63）被 `PATTERN_QUEUE_HARD_CAP`=40 截断——v120 引入时判断「正常规模触不到」，154 行库即触发。废除后 `getDueSentencesQueue` 为纯「到期卡全量（next_review_date 升序）+ 新卡预算 10 张」，与词汇/错题无上限同口径；审计模块八断言同步更新。v121 当天已冻结的 40 句快照不受影响，次日生效。
2. **三任务名统一（用户报「三个名称完全不是一个风格，不专业」）**：统一「XX打卡 · 完成今日XX」体——对练打卡 · 完成今日对练 / 句型打卡 · 完成今日句型复习（N句）/ 复习打卡 · 完成今日单词错题复习（N张）；完成任务卡文案同步「已点亮首页【句型打卡】」。
3. **学习复盘 · 重心建议（用户：近 7 天评分走势鸡肋，想要能复盘后调整学习策略重心的建议）**：按次复盘报告取代日历日折线，置「我的」页面最后一个 section。X 轴 = 练习次（范围选择器 4/8/16/全部，默认 8，切换零重拉库 `_reviewSessions` 缓存）。五块：① 范围与频率（最近 N 次 · 平均 X 天一次，≥3 天警告）② 四维按次走势（Chart.js，autoSkip maxTicksLimit 8）③ 逐维趋势结论（↑/↓/→ + 连续 2 次上升/下降警惕 + 四维最低标记）④ 反复弱点聚合（weak_areas 标签 / 语法弱点分布 / 不自然表达根因 三行 + 最新 GPT 诊断原文）⑤ 重心建议规则引擎（最多 2 条：语法类别集中度 ≥60% 且 ≥3 条 → 维度连续 2 次下降 → 最低分维度 + GPT 诊断 → nextSteps 高频 ≥2 → 平均间隔 ≥3 天频率建议）。
4. **融合删除两个独立分析区块（用户：「语法弱点分析」和「不自然表达分析」感觉没啥用，融合进学习复盘）**：`showErrorPatterns` / `showErrorDetail` / `renderExpressionInsights` / `showExpressionDetail` / `_expressionStats` 物理删除（grep 零残留）；保留共用件 `EXPRESSION_CAUSES` / `classifyExpressionCause`（提升区 + 复盘根因统计，历史日报无 pattern 启发式回退同口径）/ `aggregateGrammarCategories`（提升区）。分布口径从「errors 表全历史 + reports 全历史」统一为「复盘窗口内 mistakes 聚合」，随范围选择器走。

## 二、发布校验

- **版本三连对齐**：`?v=121→122`（index.html 4 处 + sw.js FILES 5 处）、`voco-v131→132`（sw.js CACHE）、SW 注册 `121→122`（app.js）。
- **node --check**：app.js 全过；sw.js / parser.js / supabase-client.js 本批未动（sw.js 仅版本串）。
- **提交推送**：commit `4bbf5cc` → push main 成功。
- **线上轮询验证全绿**：index.html 服务端 `v=122`、sw.js `voco-v132`、app.js 复盘模块标记全部命中。

## 三、已知取舍（沿用，记录不修）

- 复盘数据源 = reports 表按次解析（`loadReviewSessions` 缓存），naturalness/vocabulary 对旧日报有回退估算（fluency×0.8 / 词数×20），新日报直接读 GPT 四维分。
- 重心建议规则引擎为 v1 试验版：用户「先做个版本我们试试，现在没看到结果还不能给出具体意见」。
